# Browser Automation Requirements
# 浏览器自动化依赖

# 核心浏览器控制
playwright>=1.40.0

# 鼠标键盘模拟（可选，用于更高级的行为模拟）
pyautogui>=0.9.54

# 图像处理（可选，用于图像识别场景）
opencv-python>=4.8.0
pillow>=10.0.0

# 环境配置
python-dotenv>=1.0.0

# 安装说明:
# 1. pip install -r requirements.txt
# 2. playwright install
